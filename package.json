{
  "name": "testing-application",
  "version": "1.0.0",
  "description": "clear project with mocha and chai",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --timeout 10000 --reporter mocha-allure-reporter",
    "report": "allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "author": "orabovskyi",
  "license": "ISC",
  "dependencies": {
    "allure-commandline": "^2.0.0",
    "mocha-allure-reporter": "^1.2.0",
    "chai": "^4.2.0",
    "mocha": "^6.2.2"
  }
}
